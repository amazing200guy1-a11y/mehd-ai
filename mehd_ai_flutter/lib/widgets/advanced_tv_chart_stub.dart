import 'package:flutter/material.dart';

class AdvancedTvChart extends StatelessWidget {
  final String symbol;
  
  const AdvancedTvChart({super.key, required this.symbol});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text("TradingView requires Web"));
  }
}
