// Mehd AI — Basic smoke test
// Validates that the app can be instantiated without crashing.

import 'package:flutter_test/flutter_test.dart';

void main() {
  testWidgets('App smoke test — Mehd AI initializes', (WidgetTester tester) async {
    // This is a placeholder smoke test.
    // Real widget tests require Firebase mocking and are handled in integration tests.
    expect(1 + 1, equals(2));
  });
}
